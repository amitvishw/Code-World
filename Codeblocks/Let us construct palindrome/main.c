#include<stdio.h>
#include<string.h>
int main()
{
    char a[100009];
    int t,l,i;
    scanf("%d",&t);
    while(t--)
    {
        int ct=0;
        scanf("%s",a);
        h=strlen(a)-1;
        l = 0;
        while (h > l)
        {
            if (str[l++] != str[h--])
            {

            }
        }
    }
}
